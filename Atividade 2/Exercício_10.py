# Exercício 10:

dist = float(input("Digite a distância entre as duas cidades: "))
tempo = float(input("Digite o tempo da víagem entre as cidades: "))

vm = dist / tempo

print("A velocidade necessária para chegar a cidedade é de {:.2f} km/h" .format(vm))
